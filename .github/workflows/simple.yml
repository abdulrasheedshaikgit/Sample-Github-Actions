name: first-workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "hello world"
      - name: step2
        run: echo "welcome"
  second-job:
    runs-on: ubuntu-latest
    needs: first-job
    steps:
      - name: step3
        run: echo "Job 2 Step 3"
